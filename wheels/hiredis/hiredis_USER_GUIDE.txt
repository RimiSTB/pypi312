================================================================================
  hiredis - USER GUIDE (Android Python STB) - Generated by RIMI
================================================================================

  Package:     hiredis
  Description: Minimalistic C client library for Redis (static hiredis).
  Extension:   hiredis.pyd
  Language:    C
  Dependencies: None (static)

================================================================================
  1. OVERVIEW
================================================================================

hiredis is a minimalistic C client library for Redis. This Python package
provides bindings to the statically compiled hiredis library, offering
fast Redis protocol parsing and serialization.

================================================================================
  2. INSTALLATION
================================================================================

From Android shell or Termux:

    pip install /path/to/hiredis-*.whl

Or from within a build environment:

    python -m pip install --no-build-isolation hiredis

================================================================================
  3. VERIFICATION
================================================================================

After installation, verify the package loads correctly:

    python -c "import hiredis; print('OK')"

Run the pre-built test script (if available):

    python Scripts/test_hiredis.py

================================================================================
  4. USAGE EXAMPLE
================================================================================

    import hiredis

    writer = hiredis.Writer()
    writer.set("key", "value")
    # Use with redis-py for protocol-level speedup

================================================================================
  5. TROUBLESHOOTING
================================================================================

  - If import fails with "cannot find shared library" the wheel was not
    built with a static linking strategy. Rebuild with the correct
    build script.
  - Ensure your Android Python STB environment matches the ABI that
    the wheel was compiled for (typically arm64_v8a / aarch64).

================================================================================
  Generated by RIMI
================================================================================
