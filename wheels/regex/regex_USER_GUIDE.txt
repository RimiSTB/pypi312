================================================================================
  regex - USER GUIDE (Android Python STB) - Generated by RIMI
================================================================================

  Package:     regex
  Description: Regular expression engine replacement for the re module (C extension).
  Extension:   regex.pyd
  Language:    C
  Dependencies: None

================================================================================
  1. OVERVIEW
================================================================================

The regex module provides a drop-in replacement for Python's built-in "re"
module with additional features including Unicode matching, fuzzy matching,
and atomic groups. The C extension provides performance comparable to or
better than the standard library implementation.

================================================================================
  2. INSTALLATION
================================================================================

From Android shell or Termux:

    pip install /path/to/regex-*.whl

Or from within a build environment:

    python -m pip install --no-build-isolation regex

================================================================================
  3. VERIFICATION
================================================================================

After installation, verify the package loads correctly:

    python -c "import regex; print('OK')"

Run the pre-built test script (if available):

    python Scripts/test_regex.py

================================================================================
  4. USAGE EXAMPLE
================================================================================

    import regex

    text = "Hello World 123"
    # Basic matching
    match = regex.search(r"\d+", text)
    if match:
        print(match.group())  # 123

    # Unicode category matching
    matches = regex.findall(r"\p{L}+", text)

================================================================================
  5. TROUBLESHOOTING
================================================================================

  - If import fails with "cannot find shared library" the wheel was not
    built with a static linking strategy. Rebuild with the correct
    build script.
  - Ensure your Android Python STB environment matches the ABI that
    the wheel was compiled for (typically arm64_v8a / aarch64).

================================================================================
  Generated by RIMI
================================================================================
