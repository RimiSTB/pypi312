================================================================================
  brotli - USER GUIDE (Android Python STB) - Generated by RIMI
================================================================================

  Package:     brotli
  Description: Brotli compression library for Python (bundled brotli C library).
  Extension:   brotli.pyd
  Language:    C
  Dependencies: None (bundled)

================================================================================
  1. OVERVIEW
================================================================================

Brotli is a generic-purpose lossless compression algorithm developed by
Google. This package bundles the brotli C library source directly and
compiles it into the Python extension, requiring no external dependencies.

================================================================================
  2. INSTALLATION
================================================================================

From Android shell or Termux:

    pip install /path/to/brotli-*.whl

Or from within a build environment:

    python -m pip install --no-build-isolation brotli

================================================================================
  3. VERIFICATION
================================================================================

After installation, verify the package loads correctly:

    python -c "import brotli; print('OK')"

Run the pre-built test script (if available):

    python Scripts/test_brotli.py

================================================================================
  4. USAGE EXAMPLE
================================================================================

    import brotli

    data = b"Hello, Brotli compression on Android!"
    compressed = brotli.compress(data, quality=11)
    decompressed = brotli.decompress(compressed)
    assert decompressed == data

================================================================================
  5. TROUBLESHOOTING
================================================================================

  - If import fails with "cannot find shared library" the wheel was not
    built with a static linking strategy. Rebuild with the correct
    build script.
  - Ensure your Android Python STB environment matches the ABI that
    the wheel was compiled for (typically arm64_v8a / aarch64).

================================================================================
  Generated by RIMI
================================================================================
