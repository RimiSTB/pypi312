================================================================================
  pillow_simd - USER GUIDE (Android Python STB) - Generated by RIMI
================================================================================

  Package:     pillow_simd
  Description: SIMD-optimized Pillow fork for fast image processing (8 C modules).
  Extension:   pillow_simd.pyd
  Language:    C
  Dependencies: NumPy, libjpeg-turbo, libpng, zlib

================================================================================
  1. OVERVIEW
================================================================================

Pillow-SIMD is a SIMD-optimized fork of the Pillow image processing library.
It provides the same API as Pillow but with significantly improved performance
through SIMD instruction optimizations. This build produces 8 C extension
modules for comprehensive image processing support.

================================================================================
  2. INSTALLATION
================================================================================

From Android shell or Termux:

    pip install /path/to/pillow_simd-*.whl

Or from within a build environment:

    python -m pip install --no-build-isolation pillow_simd

================================================================================
  3. VERIFICATION
================================================================================

After installation, verify the package loads correctly:

    python -c "from PIL import Image; print('OK')"

Run the pre-built test script (if available):

    python Scripts/test_pillow_simd.py

================================================================================
  4. USAGE EXAMPLE
================================================================================

    from PIL import Image, ImageFilter

    # Open an image
    img = Image.open("photo.jpg")

    # Resize with SIMD-optimized resampling
    resized = img.resize((800, 600), Image.LANCZOS)

    # Apply filter
    blurred = img.filter(ImageFilter.GaussianBlur(radius=5))

    # Save
    resized.save("output.webp", "WEBP", quality=85)

================================================================================
  5. TROUBLESHOOTING
================================================================================

  - If import fails with "cannot find shared library" the wheel was not
    built with a static linking strategy. Rebuild with the correct
    build script.
  - Ensure NumPy, libjpeg-turbo, libpng, and zlib are available during
    the build process.
  - Ensure your Android Python STB environment matches the ABI that
    the wheel was compiled for (typically arm64_v8a / aarch64).
  - Note: The import name is "PIL", not "pillow_simd".

================================================================================
  Generated by RIMI
================================================================================
